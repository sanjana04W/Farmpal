rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections
    // This is needed because the app uses custom auth (username/password in Firestore)
    // rather than Firebase Authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
